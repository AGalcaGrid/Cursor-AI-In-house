import { TestAuth } from './TestAuth';

function AppTest() {
  return (
    <div style={{ minHeight: '100vh', background: '#f0f0f0' }}>
      <h1 style={{ padding: '20px', color: 'black' }}>App is Working!</h1>
      <TestAuth />
    </div>
  );
}

export default AppTest;
